def add_friend():
    new_friend={'name':" ",
                'salutation':".",
                'age' : 0,
                'rating': 0.0,
                'status':False
                }
    new_name['name']=raw_input("Enter the friend name")
    new_salutation['salutation']=raw_input("Enter the salutation")
    new_name['name']=new_salutation['salutation']+" "+new_name['name']
    new_rating['rating']=raw_input("")
    new_age['age']=raw_input("")

    new_age['age']=int(new_age['age'])
    new_rating['rating']=float(new_rating['rating'])
    if len(new_name)>0 and new_age >12 and new_age <50 and len(new_rating)>0:
        #add friend
        friends.Append
    else:
        print "invalid entry"

    return len(friends_name)